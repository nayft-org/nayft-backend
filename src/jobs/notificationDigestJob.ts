/**
 * Hourly/daily digest scheduler placeholder (RFC Phase 4).
 * Wire cron + NotificationBatch persistence when digest UX ships.
 */
export async function runNotificationDigestTick(): Promise<void> {
  // Intentionally empty for MVP — channel stubs handle no outbound digest sends.
}
