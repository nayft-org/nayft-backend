import type { FeatureConfig } from '../../core/feature-system';

export const featureConfig: FeatureConfig = {
  key: 'wishlist',
  name: 'Wishlist',
  module: 'wishlist',
  description: 'Save coins to wishlist (legacy, prefer follow)',
};
