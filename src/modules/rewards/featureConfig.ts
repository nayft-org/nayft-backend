import type { FeatureConfig } from '../../core/feature-system';

export const featureConfig: FeatureConfig = {
  key: 'rewards',
  name: 'Rewards',
  module: 'rewards',
  description: 'Reward points for user engagement',
};
