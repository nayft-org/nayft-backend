version: "3.9"

services:
  mongodb:
    image: mongo:7
    container_name: crypto-mongo
    ports:
      - "27019:27017"
    volumes:
      - mongo_data:/data/db
    environment:
      MONGO_INITDB_DATABASE: crypto_db

volumes:
  mongo_data:

